export type CardType = {
  name: string;
  bName: string;
  englishName?: string;
  bankNumber: string;
  expireMonth: string;
  expireYear: string;
  image?: string;
  isDefault: boolean;
  inventory: number;
};
